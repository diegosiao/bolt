This location must contains only Linux libraries for lin builds of the project
==============================================================================
Make sure to install:
sudo apt-get install mesa-common-dev
sudo apt-get install freeglut3
sudo apt-get install freeglut3-dev

The libs by name: 
GL 
GLU
glut